diff --git a/vendor/cryptonite/src/pkix/c/api/aid.c b/vendor/cryptonite/src/pkix/c/api/aid.c
index e092a5a..dcda07b 100644
--- a/vendor/cryptonite/src/pkix/c/api/aid.c
+++ b/vendor/cryptonite/src/pkix/c/api/aid.c
@@ -957,7 +957,8 @@ int aid_get_dstu4145_params(const AlgorithmIdentifier_t *aid, Dstu4145Ctx **ctx)
     CHECK_PARAM(ctx != NULL);
     CHECK_PARAM(aid != NULL);
 
-    if (!pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (!pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
         SET_ERROR(RET_INVALID_PARAM);
     }
 
diff --git a/vendor/cryptonite/src/pkix/c/api/oids.c b/vendor/cryptonite/src/pkix/c/api/oids.c
index 84f893a..0242f56 100644
--- a/vendor/cryptonite/src/pkix/c/api/oids.c
+++ b/vendor/cryptonite/src/pkix/c/api/oids.c
@@ -36,6 +36,10 @@ CREATE_OID_VARS(OID_PKI_GOST3411, {1, 2, 804, 2, 1, 1, 1, 1, 2, 1});
 CREATE_OID_VARS(OID_PKI_HMAC_GOST3411, {1, 2, 804, 2, 1, 1, 1, 1, 1, 2});
 CREATE_OID_VARS(OID_PKI_ASYM, {1, 2, 804, 2, 1, 1, 1, 1, 3});
 CREATE_OID_VARS(OID_PKI_DSTU4145_WITH_GOST3411, {1, 2, 804, 2, 1, 1, 1, 1, 3, 1});
+CREATE_OID_VARS(OID_PKI_DSTU7564_256, {1, 2, 804, 2, 1, 1, 1, 1, 2, 2, 1});
+CREATE_OID_VARS(OID_PKI_DSTU4145_WITH_DSTU7564, {1, 2, 804, 2, 1, 1, 1, 1, 3, 6});
+CREATE_OID_VARS(OID_PKI_DSTU4145_DSTU7564_PB_LE, {1, 2, 804, 2, 1, 1, 1, 1, 3, 6, 1, 1});
+CREATE_OID_VARS(OID_PKI_DSTU4145_DSTU7564_PB_BE, {1, 2, 804, 2, 1, 1, 1, 1, 3, 6, 1, 1, 1});
 CREATE_OID_VARS(OID_PKI_DSTU4145_PB_LE, {1, 2, 804, 2, 1, 1, 1, 1, 3, 1, 1});
 CREATE_OID_VARS(OID_PKI_SPECIAL_CURVES_PB, {1, 2, 804, 2, 1, 1, 1, 1, 3, 1, 1, 1});
 CREATE_OID_VARS(OID_PKI_DSTU4145_PB_BE, {1, 2, 804, 2, 1, 1, 1, 1, 3, 1, 1, 1, 1});
@@ -465,6 +469,10 @@ const OidNumbers *oids_get_oid_numbers_by_id(OidId oid_id)
         CASE_GET_OID_ST(OID_ETSI_QCS_QC_LIMIT_VALUE);
         CASE_GET_OID_ST(OID_PKI_GOST3410);
         CASE_GET_OID_ST(OID_GOST3410_KZ);
+        CASE_GET_OID_ST(OID_PKI_DSTU7564_256);
+        CASE_GET_OID_ST(OID_PKI_DSTU4145_WITH_DSTU7564);
+        CASE_GET_OID_ST(OID_PKI_DSTU4145_DSTU7564_PB_LE);
+        CASE_GET_OID_ST(OID_PKI_DSTU4145_DSTU7564_PB_BE);
     }
 
     return NULL;
diff --git a/vendor/cryptonite/src/pkix/c/api/oids.h b/vendor/cryptonite/src/pkix/c/api/oids.h
index 0dce6ed..fa6cb62 100644
--- a/vendor/cryptonite/src/pkix/c/api/oids.h
+++ b/vendor/cryptonite/src/pkix/c/api/oids.h
@@ -180,6 +180,10 @@ typedef enum OidId_st {
     OID_ETSI_QCS_QC_LIMIT_VALUE_ID                          = 158,    /* 0.4.0.1862.1.2 */
     OID_PKI_GOST3410_ID                                     = 159,    /* 1.2.398.3.10.1.1.1.1 */
     OID_GOST3410_KZ_ID                                      = 160,    /* 1.2.398.3.10.1.1.1.2 */
+    OID_PKI_DSTU7564_256_ID                                 = 161,    /* 1.2.804.2.1.1.1.1.2.2.1 */
+    OID_PKI_DSTU4145_WITH_DSTU7564_ID                       = 162,    /* 1.2.804.2.1.1.1.1.3.6 */
+    OID_PKI_DSTU4145_DSTU7564_PB_LE_ID                      = 163,    /* 1.2.804.2.1.1.1.1.3.6.1.1 */
+    OID_PKI_DSTU4145_DSTU7564_PB_BE_ID                      = 164,    /* 1.2.804.2.1.1.1.1.3.6.1.1.1 */
 } OidId;
 
 typedef struct OidNumbers_st {
diff --git a/vendor/cryptonite/src/pkix/c/api/pkix_utils.c b/vendor/cryptonite/src/pkix/c/api/pkix_utils.c
index 321c649..b16ee3a 100644
--- a/vendor/cryptonite/src/pkix/c/api/pkix_utils.c
+++ b/vendor/cryptonite/src/pkix/c/api/pkix_utils.c
@@ -164,8 +164,13 @@ bool is_dstu_le_params(const OBJECT_IDENTIFIER_t *oid)
 {
     LOG_ENTRY();
 
+    /* Гілка 1.2.804.2.1.1.1.1.3.6.* — ДСТУ 4145 з Купиною — дзеркалить нумерацію
+     * гілки 3.1.* (з ГОСТ 34.311): змінюється лише хеш, а кодування підпису
+     * (PB/ONB, LE/BE) те саме. Перевірено на реальних PAdES-документах Дія
+     * (signatureAlgorithm = 1.2.804.2.1.1.1.1.3.6.1.1 = PB LE). */
     return pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_PB_LE_ID))
-            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_ONB_LE_ID));
+            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_ONB_LE_ID))
+            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_DSTU7564_PB_LE_ID));
 }
 
 bool is_dstu_be_params(const OBJECT_IDENTIFIER_t *oid)
@@ -173,7 +178,8 @@ bool is_dstu_be_params(const OBJECT_IDENTIFIER_t *oid)
     LOG_ENTRY();
 
     return pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_PB_BE_ID))
-            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_ONB_BE_ID));
+            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_ONB_BE_ID))
+            || pkix_check_oid_equal(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_DSTU7564_PB_BE_ID));
 }
 
 int sign_ba_to_bs(const ByteArray *sign, const AlgorithmIdentifier_t *aid, BIT_STRING_t *sign_bitstring)
@@ -498,7 +504,8 @@ int convert_pub_key_bs_to_ba(const OBJECT_IDENTIFIER_t *oid,
 
     DO(asn_BITSTRING2bytes(pub_key_asn, &buffer, &buffer_len));
 
-    if (pkix_check_oid_parent(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (pkix_check_oid_parent(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
 
         CHECK_NOT_NULL(okey = asn_decode_with_alloc(&OCTET_STRING_desc, buffer, buffer_len));
         DO(asn_OCTSTRING2bytes(okey, &pub_key_bytes, &len));
@@ -538,7 +545,8 @@ int convert_pubkey_bytes_to_bitstring(const OBJECT_IDENTIFIER_t *signature_oid,
 
     LOG_ENTRY();
 
-    if (pkix_check_oid_parent(signature_oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (pkix_check_oid_parent(signature_oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(signature_oid, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
 
         if (is_dstu_le_params(signature_oid)) {
             DO(asn_create_octstring_from_ba(pub_key, &pub_key_os));
diff --git a/vendor/cryptonite/src/pkix/c/api/spki.c b/vendor/cryptonite/src/pkix/c/api/spki.c
index d8b6d1d..1fc0b42 100644
--- a/vendor/cryptonite/src/pkix/c/api/spki.c
+++ b/vendor/cryptonite/src/pkix/c/api/spki.c
@@ -85,7 +85,8 @@ int spki_get_pub_key(const SubjectPublicKeyInfo_t *spki, ByteArray **pub_key)
 
     DO(asn_BITSTRING2ba(&spki->subjectPublicKey, &buffer));
 
-    if (pkix_check_oid_parent(&spki->algorithm.algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (pkix_check_oid_parent(&spki->algorithm.algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(&spki->algorithm.algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
 
         DO(asn_decode_ba(&OCTET_STRING_desc, os_key, buffer));
         DO(asn_OCTSTRING2ba(os_key, &key));
diff --git a/vendor/cryptonite/src/pkix/c/crypto/cryptonite_manager.c b/vendor/cryptonite/src/pkix/c/crypto/cryptonite_manager.c
index 41cbe6e..576b349 100644
--- a/vendor/cryptonite/src/pkix/c/crypto/cryptonite_manager.c
+++ b/vendor/cryptonite/src/pkix/c/crypto/cryptonite_manager.c
@@ -12,6 +12,7 @@
 #include "oids.h"
 #include "log_internal.h"
 #include "gost34_311.h"
+#include "dstu7564.h"
 #include "aes.h"
 #include "dstu4145.h"
 #include "rs.h"
@@ -38,7 +39,10 @@ static const uint8_t CFB_WRAP_OID[13] = {0x06, 0x0b, 0x2a, 0x86,
 typedef enum DigestModeId_st {
     DIGEST_MODE_GOST34311,
     DIGEST_MODE_SHA1,
-    DIGEST_MODE_SHA2
+    DIGEST_MODE_SHA2,
+    /* ДСТУ 7564:2014 (Купина). Використовується як DigestAlgorithm у CMS/CAdES
+     * та як складова алгоритму підпису ДСТУ 4145 з Купиною (1.2.804.2.1.1.1.1.3.6.*). */
+    DIGEST_MODE_DSTU7564
 } DigestModeId;
 
 /**
@@ -50,6 +54,7 @@ typedef struct CryptoniteDigestParams_st {
         Gost34311Ctx *gost;
         Sha1Ctx *sha1;
         Sha2Ctx *sha2;
+        Dstu7564Ctx *dstu7564;
     } mode;
     AlgorithmIdentifier_t *digest_aid;  /**< ASN1 структура алгоритм хеширования */
 } CryptoniteDigestParams;
@@ -198,7 +203,8 @@ static int get_sbox_from_aid(const AlgorithmIdentifier_t *aid, ByteArray **sbox_
     CHECK_PARAM(aid != NULL);
     CHECK_PARAM(sbox_buff != NULL);
 
-    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
 
         CHECK_NOT_NULL(dstu_params = asn_any2type(aid->parameters, &DSTU4145Params_desc));
         if (dstu_params->dke) {
@@ -298,6 +304,8 @@ static int da_update(const struct DigestAdapter_st *da, const ByteArray *src)
         DO(sha1_update(digest_params->mode.sha1, src));
     } else if (digest_params->mode_id == DIGEST_MODE_SHA2) {
         DO(sha2_update(digest_params->mode.sha2, src));
+    } else if (digest_params->mode_id == DIGEST_MODE_DSTU7564) {
+        DO(dstu7564_update(digest_params->mode.dstu7564, src));
     } else {
         SET_ERROR(RET_PKIX_INVALID_CTX_MODE);
     }
@@ -333,6 +341,8 @@ static int da_final(const DigestAdapter *da, ByteArray **digest)
         DO(sha1_final(digest_params->mode.sha1, digest));
     } else if (digest_params->mode_id == DIGEST_MODE_SHA2) {
         DO(sha2_final(digest_params->mode.sha2, digest));
+    } else if (digest_params->mode_id == DIGEST_MODE_DSTU7564) {
+        DO(dstu7564_final(digest_params->mode.dstu7564, digest));
     } else {
         SET_ERROR(RET_PKIX_INVALID_CTX_MODE);
     }
@@ -371,6 +381,8 @@ static void da_free(DigestAdapter *da)
             sha1_free(digest_params->mode.sha1);
         } else if (digest_params->mode_id == DIGEST_MODE_SHA2) {
             sha2_free(digest_params->mode.sha2);
+        } else if (digest_params->mode_id == DIGEST_MODE_DSTU7564) {
+            dstu7564_free(digest_params->mode.dstu7564);
         }
         ASN_FREE(&AlgorithmIdentifier_desc, digest_params->digest_aid);
         free(digest_params);
@@ -443,7 +455,17 @@ int digest_adapter_init_by_aid(const AlgorithmIdentifier_t *aid, DigestAdapter *
     CALLOC_CHECKED(adapter, sizeof(DigestAdapter));
     CALLOC_CHECKED(digest_parameters_str, sizeof(CryptoniteDigestParams));
 
-    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))
+            || pkix_check_oid_equal(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU7564_256_ID))) {
+        /* ДСТУ 7564 (Купина) НЕ параметризована: ні sbox, ні sync, на відміну від
+         * ГОСТ 34.311. Тому контекст будується напряму, без get_sbox_from_aid. */
+        digest_parameters_str->mode_id = DIGEST_MODE_DSTU7564;
+        CHECK_NOT_NULL(digest_parameters_str->mode.dstu7564 = dstu7564_alloc(DSTU7564_SBOX_1));
+        DO(dstu7564_init(digest_parameters_str->mode.dstu7564, 32));
+        ASN_ALLOC(digest_parameters_str->digest_aid);
+        DO(pkix_set_oid(oids_get_oid_numbers_by_id(OID_PKI_DSTU7564_256_ID),
+                        &digest_parameters_str->digest_aid->algorithm));
+    } else if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
             || pkix_check_oid_equal(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_GOST3411_ID))) {
         DO(aid_create_gost3411(&digest_parameters_str->digest_aid));
 
@@ -520,6 +542,12 @@ int digest_adapter_init_by_cert(const Certificate_t *cert, DigestAdapter **da)
     CHECK_PARAM(da != NULL);
 
     if (pkix_check_oid_parent(&cert->signatureAlgorithm.algorithm,
+            oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
+        /* Сертифікат підписаний ДСТУ 4145 з Купиною: хеш визначає САМЕ алгоритм
+         * підпису, а не SPKI (тип ключа лишається 1.2.804.2.1.1.1.1.3.1.1 і для
+         * ГОСТ-, і для Купина-підписів). */
+        DO(digest_adapter_init_by_aid(&cert->signatureAlgorithm, da));
+    } else if (pkix_check_oid_parent(&cert->signatureAlgorithm.algorithm,
             oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
         DO(digest_adapter_init_by_aid(&cert->tbsCertificate.subjectPublicKeyInfo.algorithm, da));
     } else if (pkix_check_oid_equal(&cert->signatureAlgorithm.algorithm, oids_get_oid_numbers_by_id(OID_ECDSA_WITH_SHA224_ID)) ||
@@ -562,6 +590,14 @@ DigestAdapter *digest_adapter_copy_with_alloc(const DigestAdapter *da)
         CHECK_NOT_NULL(params->mode.sha1 = sha1_copy_with_alloc(ctx->mode.sha1));
     } else if (ctx->mode_id == DIGEST_MODE_SHA2) {
         CHECK_NOT_NULL(params->mode.sha2 = sha2_copy_with_alloc(ctx->mode.sha2));
+    } else if (ctx->mode_id == DIGEST_MODE_DSTU7564) {
+        /* Upstream dstu7564 не експортує copy_with_alloc, тому клонувати
+         * ПРОМІЖНИЙ стан Купини неможливо. Свідомо повертаємо помилку замість
+         * створення свіжого контексту: мовчазна підміна дала б копію з іншим
+         * станом, тобто інший дайджест. Продакшн-споживачів у pkix немає —
+         * digest_adapter_copy_with_alloc викликається лише з upstream utest,
+         * які в цій збірці вимкнені (patch 0001). */
+        SET_ERROR(RET_PKIX_UNSUPPORTED_OID);
     } else {
         SET_ERROR(RET_PKIX_UNSUPPORTED_DIGEST_ALG);
     }
@@ -3190,7 +3226,8 @@ int get_gost28147_cipher_params(const AlgorithmIdentifier_t *aid, OCTET_STRING_t
     CHECK_PARAM(aid != NULL);
     CHECK_PARAM(dke != NULL);
 
-    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))) {
+    if (pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_GOST3411_ID))
+            || pkix_check_oid_parent(&aid->algorithm, oids_get_oid_numbers_by_id(OID_PKI_DSTU4145_WITH_DSTU7564_ID))) {
         CHECK_NOT_NULL(d_params = asn_any2type(aid->parameters, &DSTU4145Params_desc));
 
         if (d_params->dke != NULL) {
